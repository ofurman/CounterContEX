# Local TabPFN core (editable install from repo root) — only needed if you're
# editing this repo's own tabpfn fork. Commented out by default: the
# zeroshot_cf experiment (including run_full_benchmark.py) only uses tabpfn's
# and tabpfn-extensions' public API, and this repo's pyproject.toml pins
# itself as tabpfn==8.0.8, which conflicts with tabpfn-extensions requiring
# tabpfn>=8.1.0. Uncomment (and drop the `tabpfn` line below) if you need the
# local fork instead of a normal PyPI tabpfn install.
# -e .
tabpfn>=8.1.0

# TabPFN extensions — unsupervised module (imputation/generation)
tabpfn-extensions[all] @ git+https://github.com/PriorLabs/tabpfn-extensions.git

# Counterfactuals evaluation harness
cel @ git+https://github.com/ofurman/counterfactuals.git

# Utilities
scikit-learn>=1.2.0
pandas>=1.4.0
numpy>=1.21.6
matplotlib>=3.5

# DiCE baseline (dice_baseline.py) only — not needed for the TabPFN experiments
dice-ml>=0.11

# W&B logging (run_full_benchmark.py) only — pass --no-wandb to skip this dependency
wandb>=0.25.1
