# Local TabPFN core (editable install from repo root)
-e .

# TabPFN extensions — unsupervised module (imputation/generation)
tabpfn-extensions[all] @ git+https://github.com/PriorLabs/tabpfn-extensions.git

# TabICL backend — includes the experimental TabICLUnsupervised estimator.
# Pin the version because the adapter deliberately reuses loaded model weights
# across query-specific kNN contexts via fitted attributes of that estimator.
tabicl==2.1.1

# Counterfactuals evaluation harness
cel @ git+https://github.com/ofurman/counterfactuals.git
dice-ml==0.12

# Utilities
scikit-learn>=1.2.0
pandas>=1.4.0
numpy>=1.21.6
matplotlib>=3.5
setuptools>=70,<81
