# Journal

Append-only. Newest entries at the bottom. Never rewrite an earlier entry.

One entry per invocation, in this shape:

```
## YYYY-MM-DD HH:MM -- Stage N: [Name] -- DONE
**Did**: [1-3 lines]
**Verification**: GATE lines passed. REPORT values: [metric]=[value]
**Provenance**: [per measured GATE: the input the value was read from, and the defect that would
turn it red] - [or `NOT MEASURED` for any that could not be produced from this run's own inputs]
**Problems**: [symptom -> root cause -> resolution -> inline/subagent] or "none"
**Commit**: `abc1234`
```

---

## 2026-09-10 02:16 -- Stage 1: Freeze sampling contracts and witnesses -- DONE
**Did**: Added a pure NumPy proposal-policy kernel with stable SHA-256-keyed uniforms, fixed equal-mass bin truncation, categorical truncation, log-space pi-beta weights, resampling, and post-projection no-op/duplicate accounting. Added independent analytical fixtures and a minimal authenticated nine-quantile witness from the preserved E3 HELOC trace.
**Verification**: GATE passed: the focused proposal-policy suite passed `17` tests, scoped Ruff passed, and `git diff --check` passed. The full repository suite passed `332` tests with `5` pre-existing warnings. An independent verifier returned PASS after mutation-oriented review. REPORT values: analytical_fixture_sha256=`75d3ac1e10c8ba1a70a7afd62987d904826f4e9c61b86623bf8c22144a3aa66f`, e3_witness_fixture_sha256=`d9cf18eb60874ed62e2171600ad651b56d948edab87d9ee9394cbd3094581b66`, historical_source_sha256=`d50d476bb5d476f96cd39cf757b59a0a68be5164b12f74a85b502ac90324fb8a`, historical_complete_sha256=`49c50de0e41be3d31817e895135fd5c2f4f8afddbfe68b32a2263edf7db1f687`, historical_trace_analysis_sha256=`6596636a03da030d13f681e6169b79b4ed9173dc92b55f855e75212131bc0eda`, baseline_raw/projected/unique_classifier/no_op/duplicate=`5/5/2/2/1`. The analytical fixture defines categorical `q=[.45,.30,.20,.05]`, classifier scores `[.10,.80,.40,.20]`, hand-normalized beta-one weights `[.12,.64,.213333,.026667]`, an eight-bin density-order witness, and projection rows containing one-hot, immutable, no-op, and duplicate cases.
**Provenance**: The focused GATE reads the committed analytical JSON and E3 witness plus the live policy kernel; wrong normalization or an extra q factor changes hand-derived weights, log normalizer, or ESS, process-dependent RNG changes the golden canonical-bytes/SHA-256/uniform triple, and dropped or misclassified projected rows change explicit reverse links and all five budget counts. Historical provenance was reauthenticated from `experiments/zeroshot_cf/results/diagnostics/tabicl-empirical-20260908/heloc-v2/00_tabicl_common.json`, its `COMPLETE.json` inventory entry, and the campaign `trace-analysis.json`; a missing member from a partially materialized bundle, hash mismatch, wrong inventory link, selector drift, or changed nine values turns the test red. If the complete ignored source bundle is absent in another checkout, the source reauthentication test reports an explicit skip while the committed hash and value witness remains mandatory. Ruff reads both changed Python files; style/import defects turn it red. The full suite reads all collected repository tests and the changed imports; collection or behavioral regression turns it red.
**Problems**: The first within-bin tail test exposed IEEE rounding of the declared final-bin expression to exactly `1.0` -> capped the final coordinate at `nextafter(1, 0)` while retaining the protocol's `2^-53` within-bin clip -> inline. The first independent review found missing golden RNG evidence, untested normalizer/ESS, and optional-only E3 authentication -> added immutable golden values, record validation, and explicit source/COMPLETE/trace hash verification, then obtained verifier PASS -> subagent.
**Commit**: `pending`

## 2026-09-10 03:08 -- Stage 2: Expose a portable TabICL distribution API -- DONE
**Did**: Added a validated NumPy-only `NumericalDistribution` record and explicit capability to the proposal-session boundary. Adapted the TabICL sampler to evaluate caller-provided ICDF levels and matching log probabilities in one guarded imputation call, forwarded the operation through the mixed categorical codec, and kept empirical/TabPFN full-distribution support explicitly unavailable. Preserved the historical grid API and added an authenticated full-search E3 replay.
**Verification**: GATE passed: the exact focused command passed `62` tests with no skips, scoped Ruff and `git diff --check` passed, and the full repository suite passed `341` tests with `5` pre-existing warnings. An independent verifier returned PASS after the replay, density, and capability witnesses were strengthened. REPORT values: numerical-only batch=`3` query/feature pairs x `1` confidence x `2` quantiles in `1` imputation call; mixed-codec batch=`2` original action units mapped to compact columns `(0,1)` with identical grid/full values in one call per API; authenticated replay=`4` batched proposal calls covering `17,136,136,136` state/feature pairs and `5` traced classifier batches; session concurrency=`serial`, protected by a process-local re-entrant lock because the fitted TabICL sampler and callback bridge are mutable.
**Provenance**: The ICDF/log-density tests read analytical fake distributions, including unequal interval slopes where the expected log density is `-log(23)`, so an incorrect ICDF, detached density, constant-zero score, batch split, ordering change, or unfilled callback turns them red. Capability tests read `TabICLBackend`, the prepared-backend dataclass default, and unsupported backends directly; dynamic capability inference or a false declaration turns them red. The replay reauthenticates `00_tabicl_trace.json` SHA-256 `a3b14d0fc9de1417e7fc574eaef6dd3cd86b80de1037ddc277ab33e8256b5995` against `COMPLETE.json`, feeds all recorded raw proposals through the live projection and diverse search, asserts every proposal/classifier call input, and compares exact candidate values plus availability/NaN padding; projection, deduplication, search, or denominator drift turns it red. The architecture AST test reads portable modules and the generic runner; a Torch/TabICL or concrete-backend dependency edge turns it red. The full suite reads all collected tests and changed imports; a collection or behavioral regression turns it red.
**Problems**: The initial fake estimator used a different missing-column traversal order from upstream TabICL -> aligned the fake with upstream ordering without changing production semantics -> subagent. The first independent review found that the E3 witness stopped before search, the density fixture was uniform, and capability support was only indirect -> replaced it with authenticated generator/search replay, added an asymmetric density witness, and froze both backend declarations directly -> inline.
**Commit**: `pending`

## 2026-09-10 04:02 -- Stage 3: Trace feature distributions through classifiers -- DONE
**Did**: Added `proposal-diagnostic-v1`: a single-pass fixed-state trace from numerical ICDF or exact categorical support through projection, duplicate/no-op linkage, one cached classifier batch, and threshold flags. Added strict per-factual SHA-256 bundles, external expected-spec/action-inventory verification, a resumable real matrix-cell driver, and E12 analysis that averages sampling banks/seeds inside factuals before classifier/target/partition/state-stratified inference. Adopted the preserved E3 trace/audit modules and their purity tests as the predecessor diagnostic layer.
**Verification**: GATE passed: focused proposal diagnostic/E12 suites passed `16` tests; the fake CLI produced byte-identical metadata, proposal, and COMPLETE payloads under `SOURCE_DATE_EPOCH=0`; scoped Ruff and `git diff --check` passed; the full repository suite passed `353` tests with `5` pre-existing warnings. The production-driver fixture resolved a matrix cell, emitted `262` numerical records (`256 + 2x3`) in one distribution call and one classifier call, then verified resume without another method prepare/session call. An independent verifier returned PASS after three substantive audit corrections. REPORT instrument values: fake conditional categorical effect=`0.35`, numerical effect=`0.135`, categorical-minus-numerical=`+0.215`; unequal-cardinality witness=`+0.4` under equal action-unit weighting; two-dataset clustered witness estimate=`+0.55`; fixed-seed bootstrap output was byte/value repeatable and emitted dataset-specific values per classifier/target/partition/state stratum. These are instrument checks, not research evidence.
**Provenance**: Trace-purity tests count the live session and classifier calls and inspect projected rows, reversed target scores, no-op mass, duplicate linkage, and atomic one-hot edits; an observer-induced call, wrong target column, projection loss, or dummy-column treatment turns them red. The writer/reader tests compare bytes across independent roots, verify externally supplied case/spec/checkpoint/context metadata and exact action-unit/bank draw inventories, and exercise terminal-row removal, coherently rehashed forgery, identity/state mutation, extra/populated roots, strict non-finite JSON, and payload corruption. The real-driver fixture reads a resolved matrix/case/runtime identity, checks a complete root-to-child hash chain, and proves verified resume skips backend work. Analytical witnesses cover categorical-dominant, numerical-dominant, tied, all-no-op, unequal-cardinality, numerical-only, reversed-target, projection-collapse, five-bank/multi-seed, explicit expected best-of-B order statistics, mixed classifiers, and hierarchical dataset/factual resampling. The architecture test reads the evaluation package and live `METRIC_SCHEMA_VERSION`; a CounterContEx dependency or evaluation-version change turns it red.
**Problems**: Initial stage implementation trusted self-declared record counts, pooled inference strata, and exposed only a fake CLI -> added externally resolved inventories, seed/bank-aware factual aggregation, and a real resumable matrix driver -> independent review. First correction still collapsed categorical records across production seeds, mislabeled an observed maximum as best-of-B, and omitted per-dataset estimates -> retained categorical seed identity, implemented the analytical IID order statistic for explicit B=9/49, and emitted dataset summaries -> independent review. Two local test/format failures were bounded and corrected under the plan's light-failure delegation rule; the second added the required fake predictor surface without changing classifier call accounting.
**Commit**: `pending`

## 2026-09-10 04:28 -- Stage 4: Integrate budgeted q decoders -- DONE
**Did**: Added immutable mode, exact-grid, IID, top-k, and top-p q-decoder configuration and routed it through the backend-neutral search bridge. Strict mode now consumes an exact per-state/action raw budget, preserves no-ops and duplicates without refill, scores globally unique rows once, accepts factual-specific sparse categorical support, and emits portable proposal, support, RNG, projected-row, reverse-link, and target-score arrays.
**Verification**: GATE passed: the corrected focused command passed `115` config/grouped/diverse/method/policy/generator/runner tests, the full repository suite passed `371` tests with `5` pre-existing warnings, scoped Ruff and `git diff --check` passed, and an independent verifier returned PASS after reproducing sparse-support and two-factual global-offset witnesses. The authenticated legacy replay remained in the green full suite, and strict mode/no-refill, mode budget, reorder, chunk, tie, and stable-source-ID witnesses passed. REPORT deterministic mixed-fixture values: grid raw/unique/raw-to-unique/oracle-rows=`18/9/0.5000/10`; IID=`18/10/0.5556/11`; top-k=`18/10/0.5556/11`; top-p=`18/10/0.5556/11`. TabICL call/row instruments were grid=`2/10`, IID=`2/10`, top-k=`3/266`, top-p=`3/266`.
**Provenance**: The focused GATE reads invalid immutable config combinations, exact q grids, keyed RNG tuples including stable factual IDs and targets, projected no-op/duplicate fixtures, strict classifier caches, real sparse `CategoryProposals` support, and persisted scored-row/target-probability tables; fallback enumeration, repeated mode, illegal truncation, ambient RNG, batch/action-order drift, dropped duplicates, an extra classifier query, fabricated categorical classes, or an undereferenceable reverse index turns a named assertion red. The legacy GATE reads the authenticated E3 nine-quantile search witness and historical fallback tests; changing default semantics or letting strict category decoding refill beyond B turns them red. The REPORT was read from a live mixed numerical/one-hot fake-backend generation for seed `42`, factual source `7`, and B=`9`; every oracle row was recorded by the test discriminator rather than inferred from a counter. The full suite reads all collected repository tests and imports; collection or cross-layer regression turns it red.
**Problems**: An initial verification command named a nonexistent proposal-policy test -> the delegated correction identified `test_proposal_sampling_policy.py` and passed `115` tests. Independent review found hidden final classifier re-scoring, ordinal factual RNG identity, order-sensitive ties, k>1 accounting drift, categorical row overcounting, rejection of valid sparse supports, and a reverse map without persisted scores -> added target-aware stable keys, canonical strict ties, global dedup/accounting, exact backend-call accounting, sparse-support decoding, and scored-row/offset/log-density artifacts, then obtained verifier PASS -> subagent plus inline fixes.
**Commit**: `pending`

## 2026-09-10 04:54 -- Stage 5: Integrate classifier-guided pi-beta sampling -- DONE
**Did**: Added validated E13 guidance settings and a finite common-pool SIR implementation. Numerical and exact categorical q pools draw M keyed samples, project before scoring, evaluate each unique row through one shared cache, weight empirical pool atoms only by `p_clf(y_t|x)^beta`, and select B with shared keyed uniforms and replacement. Added the deterministic `classifier-top-B` REPORT comparator, adaptive state-qualified pool IDs, first-validity/total oracle accounting, and portable pool, score, weight, normalizer, ESS, and selection traces.
**Verification**: GATE passed: `47` analytical/E11/E13 focused tests passed after the provenance correction; the expanded owning-layer suite passed `76` tests; the full repository suite passed `383` tests with `5` pre-existing warnings; scoped Ruff and `git diff --check` passed. Classifier-spy fixtures observed exactly two calls for a one-action numerical run (one factual row, then 64 unique pool rows) and no selected-row or final-result requery. Fixed-state beta arms shared exact q pool values, pool IDs, and resampling uniforms, while different canonical states produced distinct reproducible pool IDs. An independent verifier returned PASS. REPORT analytical categorical fixture (`q=[.45,.30,.20,.05]`, scores `[.10,.80,.40,.20]`, beta=1, 5,000 pools, B=9): M=64 mean ESS=`39.1613`, selection L1 error to exact pi=`0.01258`; M=256 mean ESS=`156.3007`, L1=`0.00511` (instrument seeds `20260974` and `20261166`).
**Provenance**: The mathematical GATE reads hand-defined q/s arrays, verifies beta-zero uniform empirical-pool weights and log normalizer zero, exact categorical inverse-CDF draws, epsilon-stable log weighting, and compares common-pool weights against selection; an extra q factor, direct support enumeration, underflow, or arm-specific random stream turns a named assertion red. The classifier-spy GATE reads every actual `predict_proba` batch and the persisted scored-row table; a second pool-row score, refill, missing pool atom, or hidden final query changes calls, rows, or reverse links. CRN fixtures compare canonical q traces, pool IDs, resampling uniforms, and divergent-parent hashes; pool aliasing or beta-dependent draws turn them red. The k=1 history witness dereferences `guidance_pool_index` into the exact quantile/value arrays; positional B/M confusion turns it red. The no-op-only fixture requires M=64 mass and B=9 consumed no-ops with one factual classifier call and no padding. REPORT frequencies were sampled directly from the declared analytical q and reweighted empirical pools; they are implementation diagnostics, not benchmark outcomes.
**Problems**: Initial full-suite review rejected a dynamic `getattr` capability probe -> made classifier classes an explicit discriminator protocol field and retained the architecture boundary. Independent review found that k=1 history indexed numerical quantiles by selected-budget position rather than M-pool position -> changed the provenance link to `pool_index`, added an exact history/final-row witness, and obtained verifier PASS. Light Ruff/fixture failures were delegated and corrected without changing scientific semantics.
**Commit**: `pending`
