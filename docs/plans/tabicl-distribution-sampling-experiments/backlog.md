# Backlog (Deferred Issues)

Each entry must be self-contained enough for a future run to pick it up cold.

| # | Title | Origin | Severity | Why deferred | Next step | Status |
|---|-------|--------|----------|--------------|-----------|--------|
| B-1 | General k>1 stopping-policy redesign | predecessor campaign B-1 | high runtime | This plan adds a strict experimental category budget for a small replication but does not resolve the existing long tail when beam search tries to fill a 16-row pool. Folding that change into E11-E13 would alter a second scientific axis. | Preserve the predecessor item; run a separately identified patience/depth/stopping comparison with returned-set guardrails. | OPEN |
| B-2 | Coordinated multi-feature TabICL proposals | planning | medium research | E11-E13 study one action unit at a time. Multiple-mask conditional completion is promising but would mix a new action operator with distribution decoding and classifier guidance. | After E11-E13, compare independent, donor-block, and TabICL block proposals at the same edit and oracle budgets. | OPEN |

Statuses: `OPEN` -> `IN_PROGRESS` -> `RESOLVED`.

When an item flips to RESOLVED, **revisit its origin stage in the same commit** -- a stage may
not stay BLOCKED on a resolved item. Summarize the fix in `journal.md`. Heavy items may warrant
their own follow-up plan; link it here.
