# Stage 6: Full-Scale Budget Sweep (Exp7) — Corrected & Reproducible

**Goal**: With the Stage-1 shortcut fix in place, re-run the budget sweep at HELOC `--max-test 200` / MOONS full, regenerating **both** CSVs from one code state — producing a fully **measured** (not copied) validity-vs-budget curve and closing the P2 reproducibility gap.
**Dependencies**: Stage 1 DONE (corrected shortcut — Decision #5: do not regenerate Exp7 CSVs before Stage 1). Host preflight passing (DGX). Benefits from Stage 3 (`--beam`).

---

## Context

The predecessor Exp7 CSVs are untrustworthy on two counts: the HELOC rows for budgets 34–1000 were
**copied** by the unsound shortcut (P1), and the MOONS CSV was generated by a **pre-shortcut** code
state (P2 — its 6 rows have distinct non-zero runtimes the current code could not produce). This stage
regenerates both from the corrected Stage-1 code so every reported budget is genuinely measured and the
artefacts match the committed code.

The sweep runs at the Stage-4/Stage-5 recommended configs: MOONS `prob_ascent` + `random_both@512`;
HELOC `prob_ascent` + `knn_both@256` (per-query kNN context). If Stage 5 revises the HELOC/MOONS
recommendation, use the revised config and note it.

---

## Steps

1. **Confirm the corrected shortcut is active** (Stage 1): `_can_saturate` gates on
   `steps_max_all < current_budget`. Sanity-run `uv run pytest experiments/zeroshot_cf/tests/test_greedy.py -q`.
2. **Run Exp7 at full scale on the DGX** (detached):
   - MOONS: full test split, budget grid per `resources/grids.md` (predecessor grid 2..64).
   - HELOC: `--max-test 200`, budget grid (17..1000).
   - `--stall-eps` default `1e-6`. Use `--beam` if runtime demands; if used, keep it constant across
     the whole sweep and record it.
   - Because the fix means the shortcut now fires **only** when no point bound the current cap, expect
     more budgets to be genuinely measured than in the predecessor run (non-zero `runtime_s`). That is
     the intended behaviour — the sweep must measure, not fabricate.
3. **Regenerate artefacts**: `results/exp7_budget_{moons,heloc}.csv` (one row per budget, all measured
   unless `steps_max_all < budget` genuinely holds; effective-n column) and `results/exp7_summary.md`
   (validity-vs-budget table + honest verdict: does validity climb past |A|, and at what budget does it
   saturate — or is the plateau confirmed at n≈200?).
4. **Pull back and commit.**

---

## Verification

- [ ] Both `results/exp7_budget_{moons,heloc}.csv` regenerated from the Stage-1 corrected code; any row
      with `runtime_s=0.0` is justified by `steps_max_all < budget` for that preceding budget (spot-check
      one).
- [ ] Re-running the sweep reproduces the committed values (reproducibility — closes P2). Note: with a
      constant seed and no beam, a re-run must match; log the seed.
- [ ] `results/exp7_summary.md` states the measured validity-vs-budget trend and the saturation budget
      (or confirms the plateau below 1.0 at the larger n), with proximity/steps cost.
- [ ] `l0_count_mean` reflects **distinct** features changed (`len(set(changed))`); `steps_*` is the
      commit count — manually verify on one CF whose history has a duplicate (if any occurs).
- [ ] `true_actionability == 1.0` everywhere.
- [ ] `git diff --name-only <base>..HEAD -- src/tabpfn` empty; no `tabpfn_client` import.

---

## Expected outcomes (record actuals)

- MOONS validity likely stays near 0.82 (the deterministic near-MAP plateau; only 2 actionable
  features), now **measured** at every budget rather than copied. HELOC near 0.80–0.90. A confirmed
  plateau below 1.0 at n≈200 is a **stronger** finding than at n=15/30 — report it as such.

---

## Commit

`feat(greedy-cf): full-scale, measured budget sweep (Exp7) regenerated from corrected shortcut`
